# `schwab` Provider

`schwab` exposes `22` total tools, `0` curated and `22` raw inventory passthrough.

## Curated tools


## Raw surfaces

- `schwab.cli.read`
- `schwab.cli.write`

`schwab` also ships `20` more raw command projections. The complete structured surface lives in [catalog.json](../catalog.json) and the deployed reference explorer.
