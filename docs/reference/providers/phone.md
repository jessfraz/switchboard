# `phone` Provider

`phone` exposes `3` total tools, `3` curated and `0` raw inventory passthrough.

## Curated tools

- `phone.call.run` [stable] Make one approved information-only business call and save its encrypted transcript
- `phone.doctor` [stable] Check local phone configuration without contacting a provider
- `phone.transcripts.list` [stable] List local encrypted call records without decrypting them

## Raw surfaces

- `phone.cli.read`
- `phone.cli.write`

`phone` also ships `0` more raw command projections. The complete structured surface lives in [catalog.json](../catalog.json) and the deployed reference explorer.
